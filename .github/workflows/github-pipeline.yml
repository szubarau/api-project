name: API Test Automation

on:
  workflow_dispatch:
    inputs:
      subscription:
        description: 'Scopes'
        required: true
        default: 'regression'
        type: choice
        options:
          - smoke
          - regression

  push:
    branches:
      - main

  schedule:
    - cron: '0 8 * * 1-5'  # Будние дни в 8:00 UTC

permissions:
  checks: write
  contents: read

jobs:
  pytest-api:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Running tests with scope: ${{ github.event.inputs.subscription }}"
            pytest -m ${{ github.event.inputs.subscription }} --alluredir=allure-results --disable-warnings -v
          else
            echo "Running full test suite"
            pytest --alluredir=allure-results --disable-warnings -v
          fi

      - name: Generate Allure Report
        run: |
          allure generate allure-results --clean -o allure-report
        continue-on-error: true

      - name: Check if allure-report exists and is not empty
        run: |
          if [ -d "allure-report" ] && [ "$(ls -A allure-report)" ]; then
            echo "Allure report directory exists and is not empty."
          else
            echo "Allure report directory does not exist or is empty!"
            exit 1
          fi

      - name: List contents of allure-report
        run: |
          echo "Contents of allure-report directory:"
          ls -l allure-report/

      - name: Upload Allure Report as Artifact
        uses: actions/upload-artifact@v3
        with:
          name: allure-report
          path: allure-report/

      - name: Publish Test Report (JUnit)
        uses: mikepenz/action-junit-report@v5
        if: success() || failure()
        with:
          report_paths: 'result.xml'